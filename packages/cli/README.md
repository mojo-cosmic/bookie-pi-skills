# `@bookie/cli`

Non-interactive automation and CI entry point over `@bookie/core`. The intended command contract is defined in [SPEC-002](../../docs/specs/002-core-and-cli.md).

Do not implement canonical policy in this package and never add automatic commit or push behavior.
