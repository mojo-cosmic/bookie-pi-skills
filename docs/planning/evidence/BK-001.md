# BK-001 completion evidence

## Scope

Established the Node 24 npm workspace, TypeScript project references, strict core compiler baseline, ESLint, Prettier, root quality command, CI workflow, and placeholder package boundaries from ADR-0004.

## Red evidence

After adding the core version contract test and workspace configuration without an implementation source, `npm run check` failed during typecheck with:

```text
error TS18003: No inputs were found in config file 'packages/core/tsconfig.json'.
```

This demonstrated that the workspace could not build or satisfy the core contract before implementation.

## Green evidence

After adding the minimal core source, `npm run check` completed successfully:

```text
repository tests: 9 passed, 0 failed
core tests:       1 passed, 0 failed
typecheck:        passed
lint:             passed
format check:     passed
```

## Acceptance notes

- Root checks execute repository and workspace tests.
- TypeScript strictness and project references are active.
- Core publishes only the accepted OKF version constant; canonical profile behavior and versioning remain for BK-002 onward.
- CLI, Pi extension, and service are package boundaries only and contain no speculative runtime code.
