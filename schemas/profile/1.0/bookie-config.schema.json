{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:bookie:schema:profile:1.0:bookie-config",
  "title": "Bookie profile manifest 1.0",
  "description": "The decoded JSON representation of a profile 1.0 bookie.yaml manifest.",
  "type": "object",
  "additionalProperties": false,
  "required": ["profile", "vault", "allowed_concept_types", "policy"],
  "properties": {
    "profile": {
      "type": "string",
      "const": "1.0"
    },
    "vault": {
      "type": "object",
      "additionalProperties": false,
      "required": ["uid", "title"],
      "properties": {
        "uid": {
          "type": "string",
          "pattern": "^VLT-[0-7][0-9A-HJKMNP-TV-Z]{25}$"
        },
        "title": {
          "type": "string",
          "minLength": 1
        }
      }
    },
    "allowed_concept_types": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {
        "enum": [
          "Project",
          "Task",
          "Document",
          "Research",
          "Decision",
          "Activity",
          "Evidence",
          "Person"
        ]
      }
    },
    "policy": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "evidence_roots",
        "exclude",
        "sensitivity",
        "attachment_max_bytes"
      ],
      "properties": {
        "evidence_roots": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": {
            "$ref": "#/$defs/relativePosixPath"
          }
        },
        "exclude": {
          "type": "array",
          "uniqueItems": true,
          "items": {
            "$ref": "#/$defs/constrainedGlob"
          }
        },
        "sensitivity": {
          "type": "object",
          "additionalProperties": false,
          "required": ["classes", "excluded_classes"],
          "properties": {
            "classes": {
              "type": "array",
              "minItems": 1,
              "uniqueItems": true,
              "items": {
                "$ref": "#/$defs/sensitivityClass"
              }
            },
            "excluded_classes": {
              "type": "array",
              "uniqueItems": true,
              "items": {
                "$ref": "#/$defs/sensitivityClass"
              }
            }
          }
        },
        "attachment_max_bytes": {
          "description": "A positive byte limit validated on the decoded numeric value; source-text precision checks belong to the YAML parser/runtime.",
          "type": "integer",
          "minimum": 1,
          "maximum": 9007199254740991
        }
      }
    }
  },
  "$defs": {
    "relativePosixPath": {
      "description": "A lexical relative POSIX path with non-empty segments and Unicode scalar support, excluding dot segments, colon and percent forms, glob syntax, backslashes, C0/C1 controls, and lone UTF-16 surrogates.",
      "type": "string",
      "pattern": "^(?!/)(?![\\s\\S]*(?:^|/)\\.\\.?(?:/|$))[^/\\\\:%*?\\[\\]{}!\\u0000-\\u001F\\u007F-\\u009F\\uD800-\\uDFFF]+(?:/[^/\\\\:%*?\\[\\]{}!\\u0000-\\u001F\\u007F-\\u009F\\uD800-\\uDFFF]+)*$"
    },
    "constrainedGlob": {
      "type": "string",
      "pattern": "^(?!/)(?!.*(?:^|/)\\.\\.?(?:/|$))(?:[A-Za-z0-9._-]+|\\*|\\*\\*)(?:/(?:[A-Za-z0-9._-]+|\\*|\\*\\*))*$"
    },
    "sensitivityClass": {
      "type": "string",
      "minLength": 1,
      "maxLength": 63,
      "pattern": "^[a-z][a-z0-9]*(?:-[a-z0-9]+)*$",
      "not": {
        "enum": ["unknown", "unclassified"]
      }
    }
  }
}
